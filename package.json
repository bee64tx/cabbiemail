{
  "name": "cabbiemail",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "download:fonts": "node scripts/download-fonts.js",
    "build:assets": "node scripts/build-assets.js",
    "build": "npm run download:fonts && npm run build:assets"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
