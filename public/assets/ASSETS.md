# Assets

Drop the following source images into this directory for the build script:

- **`logo-source.png`** — the Cabbiemail logo (minimum 512×512 px)
- **`favicon-source.png`** — the favicon source (minimum 512×512 px)

The `build:assets` script will resize and optimise these into the formats required by the site.
